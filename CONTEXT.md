# COMTRADE Viewer

Web 版电力系统暂态录波分析工具，对标 CAAP2008X。本文件是**领域词汇表**，不含实现细节。

## Language

**COMTRADE**:
IEEE 标准录波文件格式，由配对的 `.cfg`（配置）与 `.dat`（数据）组成。
_Avoid_: 波形文件、录波包（泛指）

**录波**:
一次故障或事件采集的完整 COMTRADE 数据集（cfg + dat）。
_Avoid_: 文件、样本（当指业务对象时）

**通道 (Channel)**:
录波中的一路模拟量或数字量信号；有模拟通道与数字通道之分。
_Avoid_: 曲线、线（UI 用语可保留，架构文档用「通道」）

**波形 (Waveform)**:
某通道在时间轴上的采样序列，用于 Plotly 展示。
_Avoid_: 图表、图（指数据时）

**降采样 (Downsample)**:
为显示性能将高采样点序列抽取到较少点数；须注意抗混叠，否则频谱失真。
_Avoid_: 压缩、抽稀（无技术含义时）

**分析会话 (Analysis Session)**:
用户上传一组 cfg/dat 后，后端解析并供 RMS/FFT/峰值/相角差等 API 使用的逻辑会话。
_Avoid_: 全局 reader、当前文件（实现泄漏用语）

**触发点 (Trigger Point)**:
录波中标记事件发生时刻的位置，用于对标 CAAP2008X 的触发标注。
_Avoid_: 光标（光标是 UI 测量工具）

**测量光标 (Cursor)**:
用户在波形上放置的 C1/C2 参考线，用于 Δt、Δy、频率等交互测量。
_Avoid_: 触发点

**对标 (Benchmark)**:
与 CAAP2008X 或同类录波软件的功能/交互对照，用于路线图与验收。
_Avoid_: 参考、竞品（口语）

## Flagged ambiguities

- **「应用」按钮**：在通道控制里指「将勾选应用到当前子图」，不是「部署应用」。
- **「标准版 / dev」**：`main` 为稳定基线，`dev` 为日常开发分支。

## Example dialogue

> **专家**：这次上传的录波通道数不对，是 cfg 解析问题还是 dat 截断了？  
> **开发**：先看 **分析会话** 里 reader 是否完整加载；若 dat 尾帧损坏，解析器会静默截断，需在响应里标明。  
> **专家**：FFT 结果和 CAAP 不一致。  
> **开发**：检查是否做了无抗混叠的 **降采样**，以及 **通道** 是否选对。
